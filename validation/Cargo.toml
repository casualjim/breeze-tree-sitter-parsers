[package]
name = "tree-sitter-parsers-validation"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
tree-sitter = "0.26"
tree-sitter-language = "0.1"

[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
